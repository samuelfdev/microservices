server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/samuelfdev/microservices
          search-paths:
          - microservices-java/configs/{application}
          #username: SamuelFerreira
          #password: senha